{
    "version": "0.2.0",
    "configurations": 
    [ 
        {
            "name": "PowerShell",
            "type": "PowerShell",
            "request": "launch",
            "program": "${workspaceRoot}/Tests/Invoke-Tests.ps1"
            // Or to run the current file
            // "program": "${file}"
        }
    ]
}